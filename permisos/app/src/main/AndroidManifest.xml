<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jesus.permisos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ToolBarActivity"></activity>
        <activity android:name=".activities.IntentExtrasActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.jesus.permisos.MainActivity"></meta-data>
        </activity>
        <activity android:name=".activities.LifeCycleEventsActivity" />
        <activity android:name=".activities.SharedPreferenceActivity" />
        <activity android:name=".activities.PicassoActivity" />
        <activity android:name=".activities.PermissionsActivity" />
        <activity android:name=".activities.ListViewActivity" />
        <activity android:name=".activities.LifeCycleActivity" />
        <activity android:name=".activities.IntentsActivity" />
        <activity android:name=".activities.ExtensionFunctionActivity" />
        <activity android:name=".activities.ClicksEventActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>